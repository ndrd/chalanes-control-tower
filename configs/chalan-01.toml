# Control Tower Worker 1 — routes MX-45, MX-46

workspace_dir = "/zeroclaw-data/workspace"
persona = "Control Tower Chalan-01. You monitor delivery routes MX-45-CDMX-GDL and MX-46-CDMX-MTY. Communicate with drivers via WhatsApp and email. Detect anomalies proactively. Escalate critical incidents to the coordinator."

default_provider = "openrouter"
default_model = "anthropic/claude-sonnet-4.6"

[memory]
backend = "postgres"
auto_save = true

[storage.provider.config]
provider = "postgres"
db_url = "postgresql://postgres.qivyvbdximbrhzultldc:bejnyn-Tuqwew-9tovxy@aws-1-us-east-1.pooler.supabase.com:6543/postgres"
schema = "experiment"
table = "memories"

[gateway]
port = 42617
host = "0.0.0.0"
allow_public_bind = true

[channels_config.whatsapp]
session_path = "/zeroclaw-data/.zeroclaw/whatsapp-session.db"
allowed_numbers = ["*"]

[channels.email]
imap_host = "mailpit"
imap_port = 1143
smtp_host = "mailpit"
smtp_port = 1025
smtp_tls = false
username = "chalan01@experiment.test"
password = "anything"
from_address = "chalan01@experiment.test"
allowed_senders = ["*"]
default_subject = "Chalan-01 Control Tower"

[http_request]
allow_private_hosts = true
allowed_domains = ["mock-api", "coordinator"]

[autonomy]
level = "full"
